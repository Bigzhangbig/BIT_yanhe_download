[project]
name = "bit-yanhe-download"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "Flask",
    "requests",
    "python-dotenv",
    "windows-curses; platform_system == 'Windows'",
    "tencentcloud-sdk-python>=3.1.126",
]

[project.optional-dependencies]
asr = [
    "mlx-audio>=0.4.3",
    "zhconv>=1.4.3",
]
whisper = [
    "mlx-audio>=0.4.3",
    "zhconv>=1.4.3",
]

[dependency-groups]
dev = [
    "patchright>=1.61.2",
]
